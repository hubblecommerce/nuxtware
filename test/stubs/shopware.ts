// Test stub for the virtual `#shopware` module supplied by @shopware/nuxt-module.
//
// Only the `Schemas` type is consumed by composables under test, and only in
// type position (erased at transpile time). This stub exists so the module
// resolves during unit tests without booting Nuxt.
export type Schemas = Record<string, unknown>

export {}
