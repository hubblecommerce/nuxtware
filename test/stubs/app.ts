// Test stub for Nuxt's virtual `#app` module.
//
// Composables only import types (e.g. `CookieRef`) from `#app`, which are
// erased at transpile time. This stub exists so the module resolves at all.
export type CookieRef<T> = { value: T }

export {}
